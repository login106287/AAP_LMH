#include <bits/stdc++.h>
#define vi vector<int>
#define fi first
#define se second
#define FOR(i,a,b,c) for (int i=a; i<b; i+=c)
#define FOD(i,a,b,c) for (int i=a; i>b; i-=c)
#define map<char,int> mci
#define endl "\n"
using namespace std;

main() {
    cin.tie(0)->sync_with_stdio(false);

    string s;
    cin.ignore();

    getline(cin, s);

    mci m;


    if () cout << "CHAT WITH HER!" << endl;
    else cout << "IGNORE HIM!" << endl;

    return 0;
}
