#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve(ll n) {
    vector<ll> vecA;
    ll A[n], leftNum, RightNum, Numcount;
    for (int i=0; i<n; i++) {
        cin >> A[i];
        vecA.push_back(A[i]);
    }
    for (int i=0; i<n; i++) {
        Numcount = 0;

    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    ll n;
    cin >> n;
    solve(n);


    return 0;
}
