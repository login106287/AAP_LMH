#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x = 3.12, y = 4, z;
    //z = max(x,y);
    //z = min(x,y);
    //z = pow(x,2);
    //z = sqrt(y);
    //z = abs(-3);
    //z = round(x);
    //z = ceil(x);
    //z = floor(x);
    cout << z;
    return 0;
}


