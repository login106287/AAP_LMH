#include <bits/stdc++.h>
using namespace std;

bool check(string n, vector<string>vs) {
    for (int i=0; i<arr.size();i++) {
        if (arr[i]==n) return 0;
    }
    return 1;
}

int main()
{
    string s;
    cin >> n;
    for (int i=0;i<s.size();i++) {
        for (int j=1;j<=s.size();i++) {
            string sub_str=substr(i,j);
            if ()
        }
    }

    return 0;
}
