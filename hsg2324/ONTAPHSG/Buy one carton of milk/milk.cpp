#include <bits/stdc++.h>
using namespace std;
#define pi pair<int,int>
#define fi first
#define se second

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);

    int n,counts=0,result=0;
    vector<pi>p(5);
    cin >> n;
    for (int i=0; i<3; i++) cin >> pi[i].fi;
    pi[0].se=6;
    pi[1].se=8;
    pi[2].se=12;
    if ()
    return 0;
}
