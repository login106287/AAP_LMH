#include <bits/stdc++.h>
using namespace std;

fstream doc, ghi;

/*int souoc(int n) {
    int counts = 0;
    for (int i = 1; i <= n; i++) {
        if (n % i == 0) counts++;
    }
    return counts;
}

void pt(int n) {
    while (n%2==0) {
        ghi << 2 << "*";
        n/=2;
    }

    for (int i=3; i<=sqrt(n); i+=2) {
        while (n%i==0) {
            ghi << i << "*";
            n=n/i;
        }
    }

    if (n>2) ghi << n << " ";
    ghi << endl;
}

void solve(int n){
    ghi << souoc(n) << " ";
    pt(n);
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    doc.open("Nto.inp", ios::in);
    ghi.open("Nto.out", ios::out | ios::trunc);

    int n, a;
    doc >> n;
    for (int i=0; i<n; i++) {
        doc >> a;
        solve(a);
    }

    doc.close();
    ghi.close();

    return 0;
}*/
int main(){
return 0;
}
