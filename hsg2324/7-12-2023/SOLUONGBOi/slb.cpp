#include <bits/stdc++.h>
using namespace std;

void solve(int k) {
    for (int i=0; i<k; i++) {
        int n, x, counts=0;
        cin >> n >> x;
        cout << x/n << endl;
    }
}

int main()
{
    int k;
    cin >> k;
    solve(k);
    return 0;
}
